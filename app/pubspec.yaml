name: app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  cloud_firestore: ^6.2.0
  firebase_ai_logic: ^0.1.0  # Firebase AI Logic for Gemini
  firebase_app_check: ^0.3.0
  firebase_auth: ^6.3.0
  firebase_core: ^4.6.0
  flutter:
    sdk: flutter
  google_generative_ai: ^0.4.0  # Direct Gemini SDK
  health: ^13.3.1
  google_fonts: ^6.2.1
  http: ^1.2.0
  image_picker: ^1.0.7
  provider: ^6.1.5+1
  tflite_flutter: ^0.12.1
  youtube_player_flutter: ^9.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
