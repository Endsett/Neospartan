import 'package:flutter/material.dart';
import 'exercise_library_screen.dart';

/// Stadion Screen - Wraps the exercise library
/// This screen serves as an entry point for the exercise library route
class StadionScreen extends StatelessWidget {
  const StadionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const ExerciseLibraryScreen();
  }
}
